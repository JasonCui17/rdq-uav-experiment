extends: oracle_left_fixed256.yaml

experiment:
  name: oracle_left_fixed256_lowres
  seed: 0

data:
  image_size: [24, 24]
  center_mask_fraction: 0.0
  train_color_jitter: 0.0

model:
  variant: rgb

train:
  epochs: 10
  batch_size: 64

evaluation:
  batch_size: 128
