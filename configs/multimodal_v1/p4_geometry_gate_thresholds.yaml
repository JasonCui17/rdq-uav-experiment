# P4 preregistration template. Formal audit is intentionally disabled until
# every threshold below is replaced by an explicit metric rule and this file
# is committed. Never fill these values after inspecting formal audit results.
thresholds_frozen: false
threshold_commit: null
thresholds: {}
